name: Validate

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  hacs:
    runs-on: "ubuntu-latest"
    steps:
      - name: 🔎 Analyse with hacs
        uses: hacs/action@main
        with:
          category: integration
